/** Deprecated sync Pollinations URLs — kept as no-ops for import safety.
 * Use resolveDestinationHeroPhoto / resolveExplorePlacePhoto instead.
 */

export function explorePlacePhotoUrl(_placeName: string, _city?: string): string {
  return '';
}

export function destinationHeroPhotoUrl(_placeName: string, _country?: string): string {
  return '';
}
